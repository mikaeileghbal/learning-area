const timeDiffUtility = {
  minutesBetween(dateA, dateB) {
    console.log(dateA, dateB);
  },
  hoursBetween(dateA, dateB) {},
  daysBetween(dateA, dateB) {},
};

timeDiffUtility.minutesBetween(1, 2);
